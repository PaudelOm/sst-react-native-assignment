# React Native Assignment (SST) — Auth + Profile + Todos

A small React Native (Expo) app implementing:
- Authentication (MockAPI + RTK Query): Login, Signup, Forgot Password
- Profile Management (MockAPI + RTK Query): View and Update Profile
- Todos (Local State Only): Create, List, Update, Delete

## Tech Stack
- React Native (Expo)
- TypeScript
- Redux Toolkit
- RTK Query
- React Navigation (Native Stack)
- react-hook-form

## Setup Instructions

### 1) Install dependencies
```bash
npm install
